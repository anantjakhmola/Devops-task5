apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: pvc-graf
  labels:
    name: pvc-graf

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  
